import DarkModeContainer from "@/components/DarkModeContainer";

const GlobalFooter = () => {
  return <DarkModeContainer className="h-full flex-center"></DarkModeContainer>;
};

export default GlobalFooter;
